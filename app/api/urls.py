from django.urls import path
from rest_framework.routers import DefaultRouter
# from .views import AppViewSet
from django.http import HttpResponse

# app_router = DefaultRouter()
# app_router.register('app', AppViewSet)
